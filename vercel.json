{
  "buildCommand": "cd textbook && npm install && npm run build && cd ../api && npm install && npx prisma db push --accept-data-loss && npx prisma generate",
  "outputDirectory": "textbook/build",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
