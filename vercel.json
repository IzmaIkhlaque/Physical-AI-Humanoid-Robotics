{
  "buildCommand": "cd textbook && npm install --legacy-peer-deps && npm run build && cd ../api && npm install --legacy-peer-deps && npx prisma db push --accept-data-loss && npx prisma generate",
  "outputDirectory": "textbook/build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
